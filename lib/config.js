var config = {};

config.basePath = '.';
config.cookieSecrets = ['This is the default cookie key—this should be replaced in user-land, but we want to offer sane defaults. Maybe this can be generated if we create a cli thingy?'];

exports = module.exports = config;