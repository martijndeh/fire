'use strict';

var fire = require('fire');
var app = fire.app('{{name}}', {
	modules: ['angular-route'],
	NODE_ENV: process.env.NODE_ENV
});

app.controller('/', function StartController() {

});

fire.start();
